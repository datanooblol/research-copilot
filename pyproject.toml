[project]
name = "research-copilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arxiv>=2.4.0",
    "streamlit[pdf]>=1.54.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
